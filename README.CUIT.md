# share_plus — vendored fork (CUiT / komune-mobile)

Salinan `share_plus` **12.0.1** yang sebelumnya hidup sebagai path dependency di
`komune-mobile/plugins/share_plus`. Dipindah ke repo sendiri supaya bisa dipakai
sebagai git dependency di `pubspec.yaml`.

- Upstream: https://github.com/fluttercommunity/plus_plugins/tree/main/packages/share_plus/share_plus
- Import awal: dari `komune-mobile` (riwayat modifikasi lokal ada di repo itu).

Bump versi = commit di sini lalu tag baru (`v12.0.1-cuit.N`), dan update `ref:`
di `pubspec.yaml` komune-mobile.
